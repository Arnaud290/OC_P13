version: 2.1
jobs:
  build:
    docker:
      - image: python:3.8
        auth:
          username: $DOCKER_USER
          password: $DOCKER_PASS
    steps:
      - checkout
      - restore_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements.txt" }}
      - run:
          name: create venv
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -r requirements.txt
      - save_cache:
          key: deps1-{{ .Branch }}-{{ checksum "requirements.txt" }}
          paths:
            - "venv"
      - run:
          name: Running tests
          command: |
            . venv/bin/activate
            python3 manage.py test
<<<<<<< HEAD
  deploy:
    machine: True
    steps:
      - checkout
      - run: 
          name: Create docker and push to docker hub 
          command: |
            echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin
            docker build -t arnaud290/python-oc-lettings-fr:${CIRCLE_SHA1:0:7} .
            docker push arnaud290/python-oc-lettings-fr:${CIRCLE_SHA1:0:7}
      - run:
          name: Push docker to Heroku
          command: |
            curl https://cli-assets.heroku.com/install.sh | sh
            docker tag arnaud290/python-oc-lettings-fr:${CIRCLE_SHA1:0:7} oc-lettings-290
            HEROKU_API_KEY=$HEROKU_API_KEY heroku container:login
            HEROKU_API_KEY=$HEROKU_API_KEY heroku container:push -a oc-lettings-290 web
            HEROKU_API_KEY=$HEROKU_API_KEY heroku container:release -a oc-lettings-290 web

workflows:
  build_test_and_deploy:
    jobs:
      - build
      - deploy:
          requires:
            - build
=======
            
workflows:
  build_test:
    jobs:
      - build
>>>>>>> issue_test
